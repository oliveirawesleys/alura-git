package labs.pm.app;

public class TesteLis {
    public static void main(String[] args) {
/*        List<Integer> n = List.of(2,3,0,8,1,9,5,7,6,4);
        int sum = n.stream().reduce(0, (n,m) -> n +m);
        int sums = n.stream().parallel().reduce(0, (n,m) -> n +m);
        int sumss = n.stream().flatMap(a -> a).reduce(0, (n,m) -> n +m);
        int sumss2 = n.parallelStream().reduce(0, (n, m) -> n +m);
        int sumssss2 = n.stream().iterate.reduce(0, (n, m) -> n +m);*/

    }
}
