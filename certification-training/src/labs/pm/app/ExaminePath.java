package labs.pm.app;

public class ExaminePath {
/*    public static void main(String[] args) {
        try {
            Path path = Paths.get("/u01/eork");

            //BasicFileAttributes at = Files.isDirectory(path);

            System.out.println(at.isDirectory());
        } catch (IOException e) {
            e.printStackTrace();
        }
    }*/
}
